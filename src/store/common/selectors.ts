import { RootState } from '../types';

export const loading = (state: RootState) => state.common.loading;

export const accountAddress = (state: RootState) => state.common.accountAddress;

export const balance = (state: RootState) => state.common.balance;

